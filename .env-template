# Environment variables for the client app.
# Note that variables, that start with "REACT_APP" prefix, are exposed to the public web.
# https://create-react-app.dev/docs/adding-custom-environment-variables

# Mandatory configuration
#
# Note: You also need to set Stripe secret key in Sharetribe Console (Build → Payments).
# Use the SAME Stripe TEST account for both keys below and Console.
#
# Publishable key: Stripe Dashboard → Developers → API keys → Publishable key (pk_test_…)
# Full key is ~100+ characters — do not truncate or use placeholders with "..."
#
REACT_APP_SHARETRIBE_SDK_CLIENT_ID=change-me
REACT_APP_STRIPE_PUBLISHABLE_KEY=
REACT_APP_MAPBOX_ACCESS_TOKEN=

# If you are using a process with privileged transitions,
# Client Secret needs to be set too. The one related to Client ID.
# You get this at Sharetribe Console (Build -> Applications -> Add new).
SHARETRIBE_SDK_CLIENT_SECRET=

# Sharetribe Integration API (server-only) — required for HQ coach approval permissions.
# Create a separate application in Console: Build → Applications → Add new → Integration API.
# Do NOT use Marketplace-only credentials here; coach approval will return 403 Forbidden.
SHARETRIBE_INTEGRATION_CLIENT_ID=
SHARETRIBE_INTEGRATION_CLIENT_SECRET=

# Or set up an alternative map provider (Google Maps). Check documentation.
# REACT_APP_GOOGLE_MAPS_API_KEY=

# Defaults
#

# Host/domain - don't use trailing slash: "/"
REACT_APP_MARKETPLACE_ROOT_URL=http://localhost:3000

# Marketplace name
# If not set, this defaults to 'Biketribe' in src/config/configDefault.js
REACT_APP_MARKETPLACE_NAME=

# Social logins && SSO
# If the app or client id is not set the auhtentication option is not shown in the Template
REACT_APP_FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=

REACT_APP_GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# This is overwritten by configuration in .env.development and
# .env.test. In production deployments use env variable and set it to
# 'production'
REACT_APP_ENV=production


# CSP. You can use value 'report' or 'block'.
# If the env varibale is missing, csp is disabled.
REACT_APP_CSP=report

# Options. Uncomment and set to test.
#

# REACT_APP_SHARETRIBE_USING_SSL=true
# SERVER_SHARETRIBE_TRUST_PROXY=true
# REACT_APP_SENTRY_DSN=change-me
# BASIC_AUTH_USERNAME=sharetribe
# BASIC_AUTH_PASSWORD=secret

# Internal PeakUp HQ admin API token (coach applications, ambassador activations, etc.).
# Server-only secret in .env — used as dev/fallback when not signed in as an HQ admin.
# HQ owners with peakUpHqAdmin profile flag or allowlisted email can access without this token.
# COACH_APPLICATION_ADMIN_TOKEN=change-me-to-a-long-random-string

# PeakUp HQ navigation & page access (/admin). Comma-separated allowlists (optional).
# Users with profile.publicData.peakUpHqAdmin === true in Console also get access.
# REACT_APP_PEAKUP_HQ_ADMIN_USER_IDS=uuid-1,uuid-2
# REACT_APP_PEAKUP_HQ_ADMIN_EMAILS=your@email.com
#
# HQ admin email for Professional signup/application notifications (server-side).
# In production/Netlify set PEAKUP_HQ_ADMIN_EMAIL (or PEAKUP_HQ_ADMIN_EMAILS) on the server.
# REACT_APP_PEAKUP_HQ_ADMIN_EMAIL is also read by the Node server in local dev.
# REACT_APP_PEAKUP_HQ_ADMIN_EMAIL=admin@peakupsports.com
# PEAKUP_HQ_ADMIN_EMAIL=admin@peakupsports.com

# This is GA4 id, which should start with 'G-' prefix.
# You should also turn "Enhanced measurements" off from GA.
# https://support.google.com/analytics/answer/9216061
# REACT_APP_GOOGLE_ANALYTICS_ID=change-me

# This adds data-domains for Plausible script
# If you add this environment variable, you should have created an account in plausible.io
# You can add multiple domains separated by comma. 
# E.g. REACT_APP_PLAUSIBLE_DOMAINS=example1.com,example2.com
# REACT_APP_PLAUSIBLE_DOMAINS=my.marketplace.com

# Cache configuration for the proxied SDK calls. 0 means no caching.
# This is applied to the asset endpoints only, and by default:
# - assets by version is cached for 1 day
# - asset by alias is not cached by default (because it would add an extra buffer before changes to marketplace config are applied)
# TTL=60
# TTL_ASSETS_BY_VERSION=86400

# PeakUp transactional email (configure one provider for ambassador welcome emails, etc.)
# Required for all providers:
# PEAKUP_EMAIL_FROM="PeakUp <welcome@peakupsports.com>"
#
# Option A — Resend (https://resend.com)
# RESEND_API_KEY=re_xxxxxxxx
#
# Option B — Postmark (https://postmarkapp.com)
# POSTMARK_SERVER_TOKEN=xxxxxxxx
# POSTMARK_MESSAGE_STREAM=outbound
#
# Option C — Webhook (e.g. Zapier "Catch Hook" → Gmail/SendGrid)
# PEAKUP_TRANSACTIONAL_EMAIL_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/...
# PEAKUP_TRANSACTIONAL_EMAIL_WEBHOOK_TOKEN=optional-bearer-token

# Debugging and reducing load from server
# VERBOSE=true
# PREVENT_DATA_LOADING_IN_SSR=true
